# JobHubMine Scraper Configuration

# Number of companies to fetch from API (default: 3)
GUPY_COMPANY_LIMIT=3

# Parallel worker threads (default: 16)
GUPY_THREADS=16

# Output directory (optional, CLI arg takes precedence)
# GUPY_OUTPUT_FOLDER=out/